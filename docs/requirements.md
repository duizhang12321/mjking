# 需求澄清文档（草稿）

请按下述结构逐项补充/修改，文档将作为实现的唯一事实来源。

## 一、背景与目标
- 背景：
- 目标：
- 范围边界（不做什么）：

## 二、用户与场景
- 用户角色：
- 使用场景：
- 成功标准（验收指标/容忍度）：

## 三、评分规则与量表
- 评分维度（示例：清晰度/构图/色彩/内容匹配等）：
- 分数范围（建议 0-100）：
- 加权/组合方式：
- 特殊规则（如自动拒绝/阈值）：

## 四、AI 服务与架构选择
- 方案 A：直连服务端（`wx.uploadFile` -> 后端 -> AI）
- 方案 B：云开发云函数（页面 -> 云函数 -> AI）
- 供应商/模型（如：识别/打分模型）：
- 费用/QPS/延迟预期：

## 五、接口协议
- 上传格式：`multipart/form-data`，字段名 `file`
- 业务参数：
- 响应结构：`{ score: number, detail?: string, version?: string }`
- 错误码与重试：
- 安全：鉴权（Token/Session）、限流、防重放：

## 六、隐私与合规
- 图片存储与生命周期：
- 本地压缩/脱敏：
- 处理同意与撤回功能：

## 七、端到端流程与 UI
- 页面流程：选择图片 -> 评分 -> 展示结果 -> 重试/反馈
- 状态与提示：加载中、失败、重试、结果说明：
- 结果展示：分数、维度拆解、版本号：

## 八、运维与监控
- 日志/埋点：
- 告警与熔断：
- 版本与灰度：

## 九、验收与测试
- 用例集：
- 边界与异常：
- 验收标准：

## 十、里程碑与计划
- M1（跑通模拟评分 + 基础 UI）：
- M2（接入真实 AI + 接口稳定）：
- M3（体验提升 + 合规就绪）：

