# 需求澄清文档

请按下述结构逐项补充/修改，文档将作为实现的唯一事实来源。

## 一、背景与目标
- 背景：开发一个辅助线下麻将的小程序
- 目标：为线下麻将玩家提供一个辅助工具，功能包括自定义/预置麻将规则并使用规则算分，开房间记账并自动关联记分规则
- 范围边界（不做什么）：

## 二、用户与场景
- 用户角色：线下麻将玩家
- 使用场景：
  - 玩家可以开房间记录线下麻将积分
  - 房间支持玩家手动输入每局积分或者关联一个自定义/预置的麻将规则然后通过拍照上传牌型自动记分
  - 自定义/预置麻将规则支持上传图片测试记分效果
- 成功标准（验收指标/容忍度）：

## 三、评分规则与量表
- 评分维度（示例：清晰度/构图/色彩/内容匹配等）：
- 分数范围（建议 0-100）：
- 加权/组合方式：
- 特殊规则（如自动拒绝/阈值）：

## 四、AI 服务与架构选择
- 上传图片并计算积分通过调用AI服务实现，目前使用火山引擎的API

